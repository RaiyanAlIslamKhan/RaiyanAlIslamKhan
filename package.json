{
  "name": "genegen-pipeline",
  "version": "0.1.0",
  "description": "GenreGen AI video generation pipeline — script-to-video engine for short-form vertical content",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "npx tsx src/pipeline/generate.ts",
    "server": "npx tsx src/api/server.ts",
    "dev": "npx tsx watch src/api/server.ts"
  },
  "dependencies": {
    "express": "^4.21.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
